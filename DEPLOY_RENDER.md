# Деплой на Render.com

## 🚀 Проект готов к деплою!

### Архитектура:
✅ **Fullstack приложение** - backend + frontend
✅ **Backend раздает статические файлы** (не нужен отдельный хостинг)
✅ **Единый деплой** - один сервис на Render

---

## Деплой на Render

### Способ 1: Через Blueprint (рекомендуется)

1. **Зайди на render.com** и войди через GitHub

2. **New + Blueprint**

3. **Подключи репозиторий** `uralsib-fin-test`

4. **Render автоматически найдет** `backend/render.yaml`

5. **Deploy!** ✨

---

### Способ 2: Вручную

1. **Зайди на render.com** → **New** → **Web Service**

2. **Подключи GitHub** репозиторий `uralsib-fin-test`

3. **Настройки:**
   - **Name:** `uralsib-fin-test`
   - **Runtime:** `Node`
   - **Branch:** `main` (или твоя ветка)
   - **Build Command:** `cd backend && npm ci && npm run build && npm run build:frontend`
   - **Start Command:** `cd backend && npm start`

4. **Deploy!** 🚀

---

## Как это работает

1. **Сборка Backend:** `npm run build`
2. **Сборка Frontend:** `npm run build:frontend`
   - Копирует `frontend/dist/` → `backend/public/`
3. **Запуск:** Express сервер слушает API и раздает статические файлы

---

## Результат

Получишь URL:
```
https://uralsib-fin-test.onrender.com
```

Этот URL будет работать как:
- **Frontend:** `https://uralsib-fin-test.onrender.com/`
- **API:** `https://uralsib-fin-test.onrender.com/api/...`

---

## Возможные ошибки и решения

### ❌ "Cannot find module"
**Решение:** Убедись что **Root Directory** = `backend`

### ❌ "Frontend build failed"
**Решение:** Проверь что `frontend/` существует и содержит `package.json`

### ❌ "Port not found"
**Решение:** В server.ts должен быть код:
```typescript
const port = process.env.PORT || 3000;
```

---

## Логи

**Смотри в Render Dashboard:**
- **Build logs** - сборка проекта
- **Deploy logs** - запуск сервера

---

## Next Steps

После успешного деплоя:
1. Открой URL
2. Проверь frontend работает
3. Проверь API (например `/api/admin/articles`)
4. Настрой переменные окружения если нужно

