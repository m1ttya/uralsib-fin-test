# Скрипты очистки БД

## 🧹 cleanup-test-users.js

Скрипт для удаления тестовых пользователей из базы данных.

### Что удаляет:
- Пользователей с тестовыми email: `@example.`, `@test.`, `@123.`, `@temp.`
- Пользователей, созданных более 3 дней назад
- Все связанные данные: refresh tokens, результаты тестов, курсы

### Как использовать:

```bash
# Перейти в папку scripts
cd scripts

# Запустить скрипт (из корня проекта)
node cleanup-test-users.js
```

### Важно:
⚠️ **Всегда делайте резервную копию БД перед очисткой!**

### Как скрипт работает:
1. Показывает всех пользователей в БД
2. Определяет, каких пользователей нужно удалить
3. Спрашивает подтверждение
4. Удаляет пользователей и связанные данные

### Ручная очистка (если скрипт не подходит):

```sql
-- Показать всех пользователей
SELECT user_id, email, name, created_at FROM users;

-- Удалить конкретного пользователя
DELETE FROM users WHERE email = 'test@example.com';
```

### Через админку:
- Откройте `/admin` (секретная ссылка - 5 кликов на логотип)
- Управление пользователями планируется, но пока недоступно

## 🔧 Другие способы:

### Прямая работа с БД
```bash
# Если используете SQLite (файл БД в backend/)
sqlite3 backend/uralsib_financial.db

# Посмотреть пользователей
SELECT * FROM users;

# Удалить пользователя
DELETE FROM users WHERE email LIKE '%@test.%';
```

### Пересоздать БД полностью
```bash
# Удалить файл БД
rm backend/uralsib_financial.db

# Перезапустить backend - БД создастся заново
```
