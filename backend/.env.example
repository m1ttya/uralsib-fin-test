# Database configuration
USE_SQLITE=true
DATABASE_PATH=./uralsib_financial.db

# Server configuration
PORT=4001
NODE_ENV=development
JWT_SECRET=dev-secret-change-me

# JWT token expiration
ACCESS_TOKEN_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# Admin (empty for demo)
ADMIN_LOGIN=
ADMIN_PASSWORD=

# PostgreSQL (not used, but keep for future)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
