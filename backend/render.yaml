services:
  - type: web
    name: uralsib-fin-test
    env: node
    rootDir: .
    plan: free
    buildCommand: |
      npm ci
      npm run build
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
