services:
  - type: web
    name: uralsib-fin-test-v2
    runtime: node
    buildCommand: |
      cd backend
      npm ci
      npm run build
      npm run build:frontend
    startCommand: |
      cd backend
      npm start
    envVars:
      - key: NODE_ENV
        value: production
