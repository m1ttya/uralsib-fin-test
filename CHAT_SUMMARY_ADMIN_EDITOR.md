# Краткая сводка: Панель инструментов редактора в админке

Дата: 2025-11-01

## Что сделано
- Панель инструментов редактора статей теперь всегда видна при редактировании:
  - Изначально ведёт себя как раньше: `position: sticky` внутри контейнера редактора.
  - Как только верх тулбара достиг бы нижней границы навигационной панели (NavBar), он «открепляется» и становится `position: fixed` сразу под NavBar, чтобы оба элемента всегда были видны.
  - При откреплении учитывается фактическая высота NavBar (по `#navbar-inner` и его родителю), чтобы корректно выставить смещение сверху.
  - Добавлена «прокладка» (spacer) высотой тулбара, чтобы исключить скачок контента при переключении sticky → fixed.

- Добавлены кнопки позиционирования изображений в контенте:
  - Выровнять слева (float: left + margin-right).
  - По центру (display: block + margin: auto).
  - Выровнять справа (float: right + margin-left).
  - Сбросить позиционирование (очистка float/display/margin).
  - Выбор изображения осуществляется кликом по нему в редакторе (запоминается «последняя выбранная» картинка).

## Технические детали
- Место изменений: `frontend/src/components/admin/ArticleEditor.tsx`.
- Логика фиксации:
  - Вычисляются `tbRect` (тулбар), `wrapRect` (контейнер редактора) и `navBottom` (нижняя граница NavBar через `headerRef`).
  - Условие открепления: когда `tbRect.top <= navBottom` и контейнер ещё ниже этой границы (`wrapRect.bottom > navBottom`).
  - В режиме fixed тулбар получает стили `top = navBottom`, а ширина/left берутся от обертки редактора, чтобы избежать «дрыгивания».

## Как проверить
1. Зайдите в админку → Статьи → Откройте редактор (создание/редактирование).
2. Прокрутите страницу:
   - Пока тулбар не упёрся в NavBar — он ведёт себя как `sticky` в карточке.
   - В момент, когда он бы заехал под NavBar, он «открепляется» и фиксируется прямо под NavBar.
3. Вставьте изображение (кнопка 🖼️), кликните по нему и попробуйте кнопки выравнивания: слева, центр, справа, сброс.

## Замечания и идеи на будущее
- Текущий редактор основан на `contentEditable` и `execCommand` (устаревающий API). Для более надёжного функционала (история, плагины, изображения) можно рассмотреть TipTap/ProseMirror или Slate.
- При желании можно заменить инлайн-стили на CSS-классы и централизовать оформление через Tailwind/стили проекта.
- Если появится собственный внутренний скролл-контейнер для админки, логику расчёта позиций стоит привязать к нему (или использовать `position: sticky` с правильным `top`).

## Следующие шаги (опционально)
- Создать Pull Request с изменениями редактора.
- Создать задачи в Jira:
  - «Открепление тулбара под NavBar в редакторе статей админки»
  - «Позиционирование изображений в редакторе статей админки»
- Описать решение в Confluence: схема переключения sticky → fixed, измерение высоты NavBar, обработка изображений.
